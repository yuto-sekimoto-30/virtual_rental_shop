<%= link_to t('users.show.title'), user_path(current_user), class: 'dropdown-item'%>
<%= link_to t('relationships.index.title'), relationships_path, class: 'dropdown-item'%>
<%= link_to t('tmdb_bookmarks.index.title'), tmdb_bookmarks_path, class: 'dropdown-item'%>
<%= link_to t('users.index.title'), users_path, class: 'dropdown-item'%>
<%= link_to t('user_sessions.destroy.title'), logout_path, class: 'dropdown-item', method: :delete %>